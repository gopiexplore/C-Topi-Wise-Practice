/*Write a program that prompts the user to enter two integer values. 
Then, use scanf to read in the values and use printf to print their sum to the console.*/
#include <stdio.h>
void main(){
    int a,b;
    scanf("%d %d",&a,&b);
    printf("%d the sum is ",a+b);
}


/*Operators:

Write a program that declares two integer variables and assigns them values. 
Then, use the addition operator to add the two variables and print the result to the console.*/
#include <stdio.h>
void main(){
    int a=10;
    int b=20;
    printf("%d",a+b);
}
